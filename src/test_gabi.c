
#include "minishell.h"

int	main(void)
{
	char *ptr;
	ptr = readline("minishell$");
	
	return (0);
}

if (the token == HEREDOC)
{
	ptr = readline(">");
	while (IT IS NOT THE DELIMITOR)
	{
		ptr = readline(">");
	}
}